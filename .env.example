# Pilot waitlist capture endpoint (Google Apps Script Web App URL).
# Copy this file to `.env` for local dev and set the same value in Vercel
# (Project → Settings → Environment Variables). Leave unset to disable capture.
VITE_WAITLIST_ENDPOINT=
